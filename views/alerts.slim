#alerts.panel
  h2
    | Alerts 
    - if @data.alerts.count > -1
      | (4 of 
      == @data.alerts.count
      | )
  ul
    - if @data.alerts.any?
      == partial(:alert, collection: @data.alerts)
    - else
      li.good
        |  No alerts to report
